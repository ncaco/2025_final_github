# 프로덕션 환경 변수 예제 파일
# 이 파일을 복사하여 .env.production 파일을 생성하고 실제 값으로 변경하세요

# ============================================
# 데이터베이스 설정
# ============================================
POSTGRES_DB=common_db
POSTGRES_USER=your_db_user
# ⚠️ 반드시 강력한 비밀번호로 변경하세요!
POSTGRES_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD

# ============================================
# 백엔드 설정
# ============================================
# 데이터베이스 연결 URL (Docker 내부 네트워크 사용)
# ⚠️ 위의 POSTGRES_USER와 POSTGRES_PASSWORD를 실제 값으로 변경한 후 여기도 수정하세요!
DATABASE_URL=postgresql://your_db_user:CHANGE_THIS_TO_STRONG_PASSWORD@postgres:5432/common_db

# JWT 시크릿 키 (최소 64자 이상의 강력한 랜덤 문자열 필수!)
# 생성 방법: python -c "import secrets; print(secrets.token_urlsafe(64))"
# ⚠️ 반드시 실제 값으로 변경하세요!
SECRET_KEY=CHANGE_THIS_TO_STRONG_RANDOM_SECRET_KEY_MIN_64_CHARS_FOR_PRODUCTION

# 애플리케이션 정보
APP_NAME=2026 Challenge Project
APP_VERSION=1.0.0
DEBUG=false

# CORS 허용 오리진 (프론트엔드 도메인)
# 쉼표로 구분하거나 JSON 배열 형식으로 작성 가능
# ⚠️ 실제 프론트엔드 도메인으로 변경하세요!
CORS_ORIGINS=["https://yourdomain.ddns.net","https://www.yourdomain.ddns.net"]

# 토큰 만료 시간 설정
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ============================================
# 프론트엔드 설정
# ============================================
# 백엔드 API URL (HTTPS 사용)
# ⚠️ 실제 API 도메인으로 변경하세요!
NEXT_PUBLIC_API_URL=https://api.yourdomain.ddns.net

# ============================================
# 도메인 설정
# ============================================
# DDNS 도메인 (예: DuckDNS, No-IP 등)
# ⚠️ 실제 도메인으로 변경하세요!
FRONTEND_DOMAIN=yourdomain.ddns.net
BACKEND_DOMAIN=api.yourdomain.ddns.net

# ============================================
# Let's Encrypt 설정
# ============================================
# 이메일 주소 (인증서 만료 알림용)
# ⚠️ 실제 이메일 주소로 변경하세요!
CERTBOT_EMAIL=your-email@example.com
