# 개발 서버 환경 변수 예제 파일
# 이 파일을 복사하여 .env.dev 파일을 생성하고 실제 값으로 변경하세요

# ============================================
# 데이터베이스 설정
# ============================================
POSTGRES_DB=common_db
POSTGRES_USER=your_dev_db_user
# ⚠️ 개발 서버 데이터베이스 비밀번호로 변경하세요!
POSTGRES_PASSWORD=CHANGE_THIS_TO_DEV_PASSWORD

# ============================================
# 백엔드 설정
# ============================================
# 데이터베이스 연결 URL (Docker 내부 네트워크 사용)
DATABASE_URL=postgresql://your_dev_db_user:CHANGE_THIS_TO_DEV_PASSWORD@postgres:5432/common_db

# JWT 시크릿 키 (개발 서버용, 프로덕션과 다르게 설정)
SECRET_KEY=dev-server-secret-key-change-in-production-min-32-chars

# 애플리케이션 정보
APP_NAME=2026 Challenge Project (Development)
APP_VERSION=1.0.0
DEBUG=true

# CORS 허용 오리진 (개발 서버 도메인)
CORS_ORIGINS=["https://dev-api.yourdomain.com", "http://localhost:3000"]

# 토큰 만료 시간 설정
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ============================================
# 프론트엔드 설정
# ============================================
# 백엔드 API URL (개발 서버)
NEXT_PUBLIC_API_URL=https://dev-api.yourdomain.com

# ============================================
# 도메인 설정
# ============================================
# 개발 서버 도메인
# ⚠️ 실제 개발 서버 도메인으로 변경하세요!
FRONTEND_DOMAIN=dev.yourdomain.com
BACKEND_DOMAIN=dev-api.yourdomain.com

# ============================================
# Let's Encrypt 설정 (개발 서버에서는 선택사항)
# ============================================
# 이메일 주소 (인증서 만료 알림용, 선택사항)
# ⚠️ 실제 이메일 주소로 변경하세요!
CERTBOT_EMAIL=your-email@example.com
