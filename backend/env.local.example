# ============================================
# 로컬 개발 환경 변수 예제 파일
# ============================================
# 이 파일을 복사하여 env.local 파일을 생성하고 실제 값으로 변경하세요
# cp env.local.example env.local

# ============================================
# 데이터베이스 설정
# ============================================
# 로컬 PostgreSQL 데이터베이스 연결
# ⚠️ 실제 로컬 데이터베이스 사용자명과 비밀번호로 변경하세요!
# 비밀번호에 특수문자(#, !, @ 등)가 포함된 경우 URL 인코딩 필요
# # -> %23, ! -> %21, @ -> %40
DATABASE_URL=postgresql://your_local_db_user:your_local_db_password@localhost:5432/common_db

# ============================================
# 애플리케이션 설정
# ============================================
APP_NAME=2026 Challenge API (Local)
APP_VERSION=1.0.0
DEBUG=True

# ============================================
# 서버 설정
# ============================================
HOST=0.0.0.0
PORT=8000

# ============================================
# 보안 설정
# ============================================
# JWT 시크릿 키 (로컬 개발용, 프로덕션에서는 반드시 변경!)
# 생성 방법: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=local-dev-secret-key-change-in-production-min-32-chars
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ============================================
# CORS 설정
# ============================================
# 로컬 개발 서버 허용
CORS_ORIGINS=["http://localhost:3000", "http://localhost:5173", "http://127.0.0.1:3000"]
